---
title: "NDI Architech"
description: "The AI-powered development orchestrator that designed and built this entire portfolio. A meta-tool that transforms ideas into production-ready codebases through intelligent architecture planning."
category: "cli-tool"
status: "completed"
featured: true
order: 0
startDate: 2024-06-01
endDate: 2024-12-15
technologies:
  - "TypeScript"
  - "Node.js"
  - "Claude AI"
  - "AST Parsing"
  - "Git"
links:
  github: "https://github.com/salvadalba/nodaysidle-ndiarchitech"
seoTitle: "NDI Architech - AI Development Orchestrator"
seoDescription: "The AI-powered development orchestrator that transforms ideas into production-ready codebases through intelligent architecture planning"
ogImage: "/og-ndiarchitech.svg"
keywords:
  - "AI"
  - "TypeScript"
  - "CLI"
  - "architecture"
  - "code generation"
sections:
  - id: "origin"
    title: "The Meta Problem"
    content: "Every project in this portfolio was built by NDI Architech. It started as a question: what if an AI could not just write code, but architect entire systems? This tool doesn't just generate files - it thinks about structure, dependencies, and long-term maintainability."
    codeBlock:
      code: |
        // The orchestrator that builds everything
        interface ArchitechConfig {
          projectName: string;
          vision: string;
          constraints: string[];
          targetStack: TechStack;
        }

        async function orchestrate(config: ArchitechConfig): Promise<Project> {
          const prd = await generatePRD(config.vision);
          const architecture = await designArchitecture(prd);
          const tasks = await decomposeTasks(architecture);
          return await executeWithValidation(tasks);
        }
      language: "typescript"
      filename: "core/orchestrator.ts"
    visual:
      type: "terminal"
      content: "architech init --vision 'portfolio that compiles visually'"
  - id: "intelligence"
    title: "Document-Driven Development"
    content: "NDI Architech generates comprehensive documentation before writing a single line of code. PRD, ARD, TRD, and TASKS.md files create a blueprint that ensures consistency and architectural integrity across the entire codebase."
    codeBlock:
      code: |
        // Generate architecture documents
        const documents = await generateDocuments({
          type: 'full-stack',
          patterns: ['islands', 'content-collections'],

          generate: {
            PRD: true,    // Product Requirements
            ARD: true,    // Architecture Requirements
            TRD: true,    // Technical Requirements
            TASKS: true,  // Implementation Tasks
            AGENT: true,  // AI Agent Guidelines
          }
        });

        // Validate consistency across all docs
        await validateDocumentConsistency(documents);
      language: "typescript"
      filename: "generators/documents.ts"
    visual:
      type: "diagram"
      content: "Document Generation Pipeline"
    revealElements:
      - id: "prd-gen"
        animation: "slide-up"
        delay: 100
      - id: "validation"
        animation: "fade"
        delay: 300
  - id: "execution"
    title: "Intelligent Task Execution"
    content: "Tasks are decomposed into atomic units with clear dependencies. The orchestrator executes them in optimal order, validating each step against the architecture documents to prevent drift."
    codeBlock:
      code: |
        // Task execution with validation
        class TaskExecutor {
          async execute(task: Task): Promise<Result> {
            // Validate against architecture docs
            await this.validateAgainstARD(task);

            // Generate code with context
            const code = await this.generateCode(task, {
              existingFiles: await this.scanProject(),
              patterns: await this.extractPatterns(),
              constraints: task.constraints,
            });

            // Verify before commit
            await this.runTests(code);
            await this.checkTypes(code);

            return this.commit(code, task);
          }
        }
      language: "typescript"
      filename: "execution/task-executor.ts"
    visual:
      type: "code"
      content: "async execute(task: Task): Promise<Result>"
      language: "typescript"
  - id: "recursion"
    title: "The Recursive Beauty"
    content: "NDI Architech built this portfolio you're viewing right now. It generated the PRD, designed the split-screen architecture, created every component, and even wrote these words. The tool that builds tools, recursively improving itself."
    codeBlock:
      code: |
        // This portfolio was generated by running:
        await architech.create({
          name: 'THE_ARCHITECT_LOG',
          vision: `A portfolio that demonstrates the craft of building
                   software by showing code transforming into visual
                   products in real-time`,
          stack: {
            framework: 'astro@5',
            islands: 'svelte@5',
            styling: 'tailwindcss@4',
          },
          features: [
            'split-screen-narrative',
            'scroll-synced-animations',
            'terminal-aesthetic',
            'syntax-highlighted-typing',
          ],
        });

        // And it just... worked.
      language: "typescript"
      filename: "examples/this-portfolio.ts"
    visual:
      type: "terminal"
      content: "architech build --self-reference true"
---

# NDI Architech

The architect that builds architects.
